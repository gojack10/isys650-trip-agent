# Langflow exports

Langflow flows are exported and imported as JSON. `trip-agent.json` is the export of the new `Trip Planning Agent — Draft` flow and should be updated through Langflow rather than hand-built as a fake graph.

The Langflow server is the runtime for the flow; this Git repository records the project history and reviewable export.
